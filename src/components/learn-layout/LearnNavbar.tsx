import { NavbarLink } from '@/model/navbar-link';

export default function LearnNavbar({ item }: { item: NavbarLink }) {
  return (
    <div className="flex justify-between bg-(#179BAE)">
      <div className="flex gap-8">
      </div>
    </div>
  );
}
